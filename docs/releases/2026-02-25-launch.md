# Release Notes - 2026-02-25

## Release
- Hosting + Functions deployed to `beaurocks-karaoke-v2`.
- Git branch pushed: `overnight/stability-hardening-pass-1`.
- Launch tag pushed: `launch-2026-02-25`.

## Included product/ops updates
- Marketing + demo surface updates currently in working branch.
- Realtime launch gates codified in `BACKLOG.md`:
  - TV as shared visual reference (not primary controller).
  - Audience/mobile as primary interaction surface.
  - Host authoritative for orchestration.
  - Event/delta sync + heartbeat snapshot policy.
  - Launch latency SLO targets.

## Validation run summary
- `npm run test:unit`: pass.
- `npm run build`: pass.
- `npm run qa:p0:host-join`: pass.
- `npm run qa:p0:users`: pass.
- `npm run qa:p0:appcheck`: pass in monitor mode (`QA_APP_CHECK_EXPECT_REJECT` unset), fail in enforce mode (`QA_APP_CHECK_EXPECT_REJECT=true`) because backend is still `APP_CHECK_MODE=log`.
- `npm run qa:overnight:smoke`: pass.
- `npm run qa:personas:prod`: pass.

## Operational findings
- Monitoring alert policies created:
  - `[Launch] Firestore Write Surge`
  - `[Launch] Firestore Read Surge`
  - `[Launch] Functions 5xx Error Burst`
  - `[Launch] Functions Request Surge`
- Billing budget created:
  - `[Launch] Monthly Firestore+Functions Guardrail` (`300 USD`, thresholds `50/80/100%`)
- Functions logs show repeated `[app-check] missing token` warnings on callable paths.
- Launch watch snapshot script executed:
  - `scripts/ops/watch-launch-signals.ps1` captured `errors=0` and `appCheckMissing=11` in the two-hour window.

## Follow-up actions (required)
1. Decide App Check cutover timing from monitor (`APP_CHECK_MODE=log`) to enforce (`APP_CHECK_MODE=enforce`), then rerun `npm run qa:p0:appcheck` with `QA_APP_CHECK_EXPECT_REJECT=true`.
2. Add notification channels to launch alert policies and re-run `scripts/ops/setup-launch-alerts.ps1`.
3. Keep 24-hour watch snapshots with `scripts/ops/watch-launch-signals.ps1`.
